apiVersion: v1
kind: Secret
metadata: 
  name: ad-generator-secret
  namespace:  default
type:  Opaque
stringData:
  # Database credentials (if needed in future)
  DB_PASSWORD: "change-me-in-production"

  # API keys (if needed)
  API_KEY: "demo-api-key-12345"
  
  # Docker registry (for private images)
  DOCKER_USERNAME: "ammanharoon"