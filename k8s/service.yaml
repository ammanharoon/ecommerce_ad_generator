apiVersion: v1
kind: Service
metadata:
  name: ad-generator-service
  namespace: default
  labels:
    app: ad-generator
spec:
  type: LoadBalancer
  selector: 
    app: ad-generator
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8000
  sessionAffinity: None