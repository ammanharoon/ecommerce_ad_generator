apiVersion: v1
kind: Service
metadata:
  name: ad-generator-service
  labels:
    app: ad-generator
spec:
  type: LoadBalancer
  selector:
    app: ad-generator  # Matches both stable and canary
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
